<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.df4j.xctec.xcms</groupId>
        <artifactId>xcms</artifactId>
        <version>${revision}</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>xcms-web-starter</artifactId>
    <packaging>jar</packaging>
    <name>xcms-web-starter</name>
    <description>XCMS Web 安全与 OpenAPI 基础 Starter（AT-29）：统一 SecurityFilterChain、JWT Resource Server、全局异常处理、OpenAPI 配置，供单体与微服务复用。</description>

    <dependencies>
        <dependency>
            <groupId>com.df4j.xctec.xcms</groupId>
            <artifactId>xcms-shared-kernel</artifactId>
        </dependency>
        <!-- 权限装载依赖 PermissionService（auth-api） -->
        <dependency>
            <groupId>com.df4j.xctec.xcms</groupId>
            <artifactId>xcms-auth-api</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>
        <!-- JwtDecoder / NimbusJwtDecoder 由资源服务器依赖提供 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-oauth2-resource-server</artifactId>
        </dependency>
        <!-- OpenAPI 配置与 UI -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>${springdoc.version}</version>
        </dependency>

        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
